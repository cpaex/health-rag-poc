# docs/

Drop the **architecture document** and **concept glossary** this spec was derived
from here (SPEC.md §13). If present, read them before Phase 1 — the spec references
specific sections:

- architecture doc §"Where your ask needs a design decision" (ontology index design)
- Step 8 — the five system-prompt rules to port verbatim (`agent/system_prompt.md`)
- Step 10 — the multi-step retrieval walkthrough to replicate as a test
- Phase 1 — the ingestion pipeline this repo mirrors

Not required to build; they explain the *why*.
